name: Deploy Backend to Google Cloud Run

on:
  push:
    branches: [main]
    paths:
      - 'server/**'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # Required for GCP authentication

jobs:
  deploy:
    name: Deploy to Cloud Run
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Build TypeScript
        working-directory: server
        run: |
          npm ci
          npm run build

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Create Artifact Registry repository
        run: |
          gcloud artifacts repositories create courtwatch \
            --repository-format=docker \
            --location=us-central1 \
            --description="CourtWatch Docker images" \
            --project=${{ secrets.GCP_PROJECT_ID }} \
            --quiet 2>/dev/null || true

      - name: Configure Docker Auth
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Get repository name
        id: repo
        run: echo "REPO=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]' | sed 's/.*\///')" >> $GITHUB_OUTPUT

      - name: Generate tags
        id: vars
        run: |
          TAG=$(date +%Y%m%d-%H%M%S)
          SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "sha=${SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "repo=${{ steps.repo.outputs.REPO }}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        working-directory: server
        run: |
          # Build for AMD64 (Cloud Run requirement)
          docker build \
            --tag us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:${{ steps.vars.outputs.tag }} \
            --tag us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:${{ steps.vars.outputs.sha }} \
            --tag us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:latest \
            --platform linux/amd64 \
            .
          
          docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:${{ steps.vars.outputs.tag }}
          docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:latest

      - name: Set Firebase secrets in Secret Manager
        run: |
          # Create or update secrets in Secret Manager
          printf '%s' "${{ secrets.FIREBASE_PRIVATE_KEY }}" | gcloud secrets create firebase-private-key --replication-policy=automatic --project=${{ secrets.GCP_PROJECT_ID }} 2>/dev/null || printf '%s' "${{ secrets.FIREBASE_PRIVATE_KEY }}" | gcloud secrets versions add firebase-private-key --data-file=- --project=${{ secrets.GCP_PROJECT_ID }}
          
          printf '%s' "${{ secrets.FIREBASE_CLIENT_EMAIL }}" | gcloud secrets create firebase-client-email --replication-policy=automatic --project=${{ secrets.GCP_PROJECT_ID }} 2>/dev/null || printf '%s' "${{ secrets.FIREBASE_CLIENT_EMAIL }}" | gcloud secrets versions add firebase-client-email --data-file=- --project=${{ secrets.GCP_PROJECT_ID }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ steps.vars.outputs.repo }}-api \
            --image us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/courtwatch/server:${{ steps.vars.outputs.tag }} \
            --platform managed \
            --region us-central1 \
            --allow-unauthenticated \
            --set-env-vars DB_TYPE=firestore,GCP_PROJECT=${{ secrets.GCP_PROJECT_ID }} \
            --update-secrets=FIREBASE_PRIVATE_KEY=firebase-private-key:latest,FIREBASE_CLIENT_EMAIL=firebase-client-email:latest \
            --service-account ${{ secrets.GCP_SERVICE_ACCOUNT }} \
            --memory 256Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 1 \
            --concurrency 80

      - name: Get Cloud Run URL
        id: cloudrun
        run: |
          URL=$(gcloud run services describe ${{ steps.vars.outputs.repo }}-api \
            --platform managed \
            --region us-central1 \
            --format 'value(status.url)')
          echo "url=${URL}" >> $GITHUB_OUTPUT
          echo "URL=$URL" >> $GITHUB_ENV

      - name: Summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ steps.vars.outputs.repo }}:${{ steps.vars.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "**Service URL:** ${{ env.URL }}" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** Deployed successfully ✅" >> $GITHUB_STEP_SUMMARY